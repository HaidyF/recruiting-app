<%= flash[:message]%>
<h1> All Users </h1>
<% @users.each do |u| %>
<%= link_to u.username + " - " + u.email, user_path(u) %>
<br>
<% end%>
<br>
<%= link_to "New User", new_user_path(@user) %>
<br>
<%= link_to "Home", root_path(@user) %>